# BallerApp Design Manifest

Copy from `design-manifest.template.md` when starting. Update after each Pencil session.

Status: **not started** — create `baller-app.pen` first.

See `.cursor/project/design-pipeline.md`.
