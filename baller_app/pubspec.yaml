name: baller_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  supabase_flutter: ^2.0.0
  flutter:
    sdk: flutter
  image_picker: ^1.2.0
  http: ^1.5.0
  dio: ^5.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/badwords.txt
    - assets/icons/
